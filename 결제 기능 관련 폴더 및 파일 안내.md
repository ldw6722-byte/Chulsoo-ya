# 결제 기능 관련 폴더 및 파일 안내

## 현재 결제 흐름

`구매자 주문 요청 → 판매자 응찰·재고 확인 → PAYMENT_PENDING → 관리자 개발 결제 승인 → PAID → PREPARING → 배송/픽업 처리`

현재의 **개발 결제 승인**은 실제 카드·계좌 이체를 만들지 않는다. 개발 환경에서 판매자 응찰 이후 주문 상태 전환을 확인하기 위한 임시 대체 흐름이다. 운영 전에는 동일 지점을 PG 결제 승인 API와 웹훅 처리로 교체한다.

## 프론트엔드

| 경로 | 역할 | 수정 시점 |
| :--- | :--- | :--- |
| `client/src/features/my/PaymentMethodsPage.tsx` | 마이철수 결제수단 탭. 계좌·신용/체크카드 목록, 등록·삭제, 단계형 팝업 UI를 담당한다. 전체 번호는 저장하지 않는다. | 결제수단 화면·은행 목록·카드사 목록·등록 팝업을 바꿀 때 |
| `client/src/features/admin/DevelopmentPaymentApprovalPanel.tsx` | 관리자 왼쪽 메뉴의 **개발 검증 → 개발 결제 승인** 화면. `PAYMENT_PENDING` 주문을 보여주고 개발용 승인 버튼을 제공한다. | 개발 승인 목록·문구·상태 표시를 바꿀 때 |
| `client/src/features/admin/AdminOverviewPage.tsx` | 관리자 왼쪽 메뉴, `AdminView`, 탭 제목·설명, 탭별 패널 렌더링을 관리한다. | 관리자 새 탭 추가·이름 변경·정렬을 바꿀 때 |
| `client/src/features/checkout/CheckoutPage.tsx` | 구매자 주문 요청과 배송지·매칭 지역 선택을 담당한다. 실제 PG 결제창 호출 시작점이 된다. | 결제하기 버튼을 PG 결제창으로 교체할 때 |
| `client/src/features/checkout/PaymentPage.tsx` | 현재 결제 완료/결과 확인 화면이다. | PG 승인 결과·실패·취소 화면을 보완할 때 |
| `client/src/api/endpoints.ts` | Axios REST API 계약. `paymentMethodApi`, `adminPaymentApi`, `orderApi`가 있다. | API URL·요청·응답 계약을 변경할 때 |
| `client/src/types/api.ts` | `PaymentMethod`, `Order`, `PaymentRefundView` 등의 타입 정의다. | API 응답 필드가 변경될 때 |
| `client/src/app/router.tsx` | `/my/payment-methods` 결제수단 페이지의 로그인 전용 라우트를 등록한다. | 결제 관련 새 페이지 라우트를 추가할 때 |

## 백엔드: 주문·결제 상태 전환

| 경로 | 역할 | 실제 PG 전환 시 처리 |
| :--- | :--- | :--- |
| `server/src/main/java/com/chulsooya/server/domain/order/PaymentController.java` | 구매자 결제 확인 API. 현재는 개발 PG 스텁으로 Payment 생성·결제 완료·준비 중 전환을 처리한다. | 서버에서 PG `/payments/confirm` 승인 API를 호출하고, 성공 응답을 검증한 뒤 상태 전환하도록 교체 |
| `server/src/main/java/com/chulsooya/server/domain/order/DevelopmentPaymentApprovalService.java` | 관리자 전용 개발 결제 대기 목록·임시 승인 서비스다. `PAYMENT_PENDING → PAID → PREPARING`을 처리한다. | 운영 전에는 비활성화·제거하거나 개발 프로필에서만 노출. PG 승인 성공 처리와 혼용 금지 |
| `server/src/main/java/com/chulsooya/server/domain/order/Payment.java` | 결제 엔티티. 금액, 잔액, 상태, 거래 키, 결제 방식을 보관한다. | PG `paymentKey`, 승인 시각, 결제수단의 마스킹 정보 등 실제 승인 응답 필드를 반영 |
| `server/src/main/java/com/chulsooya/server/domain/order/PaymentRepository.java` | 주문·결제 키 기준 결제 조회와 잠금에 사용한다. | PG 거래 키·주문 번호의 멱등성 검증 쿼리가 필요할 때 확장 |
| `server/src/main/java/com/chulsooya/server/domain/order/PaymentRefundService.java` | 관리자 환불·부분 환불·감사 이력을 처리한다. | 개발 취소 키 대신 PG 취소 API 응답 키와 멱등 키를 사용하도록 교체 |
| `server/src/main/java/com/chulsooya/server/domain/order/PaymentRefund.java` | 환불 요청·성공 상태·처리자·사유를 보관한다. | PG 취소 거래 키·실패 코드가 필요하면 필드 확장 |
| `server/src/main/java/com/chulsooya/server/domain/order/Order.java` | 주문 상태 머신. `PAYMENT_PENDING`, `PAID`, `PREPARING` 전이를 정의한다. | 상태 머신은 유지. PG 성공 응답 또는 검증된 웹훅만 `markPaid`를 호출 |
| `server/src/main/java/com/chulsooya/server/domain/order/OrderStatus.java` | 주문 상태 전이 허용 규칙이다. | 새 비동기 결제 상태가 필요할 때만 확장. 임의 전이 추가 금지 |
| `server/src/main/java/com/chulsooya/server/domain/order/OrderRepository.java` | 주문 조회·잠금·개발 결제 대기 목록 조회를 제공한다. | PG 상태 대사·미승인 주문 배치 점검이 필요할 때 확장 |
| `server/src/main/java/com/chulsooya/server/domain/admin/AdminPaymentController.java` | 관리자 환불 API와 개발 결제 대기 목록·승인 API를 제공한다. | 개발 승인 엔드포인트는 운영 비활성화. 환불은 PG 취소 API 호출로 전환 |

## 백엔드: 결제수단 등록

| 경로 | 역할 | 주의사항 |
| :--- | :--- | :--- |
| `server/src/main/java/com/chulsooya/server/domain/paymentmethod/PaymentMethod.java` | 사용자 결제수단의 유형·은행/카드사·마지막 네 자리만 저장한다. | 카드·계좌 전체 번호, CVC, 비밀번호는 저장하지 않는다. |
| `server/src/main/java/com/chulsooya/server/domain/paymentmethod/PaymentMethodRepository.java` | 로그인 사용자 본인의 결제수단 목록·삭제에 사용한다. | 사용자 소유권 필터를 유지한다. |
| `server/src/main/java/com/chulsooya/server/domain/paymentmethod/PaymentMethodService.java` | 등록·조회·삭제와 마지막 네 자리 마스킹을 처리한다. | PG 토큰/빌링키 방식 도입 시 전체 번호 대신 토큰만 추가 저장한다. |
| `server/src/main/java/com/chulsooya/server/domain/paymentmethod/PaymentMethodController.java` | `/api/payment-methods` 본인 전용 REST API다. | 실제 자동결제 기능은 PG가 발급한 토큰/빌링키 API를 별도 연동한다. |
| `server/src/main/java/com/chulsooya/server/domain/paymentmethod/PaymentMethodDtos.java` | 결제수단 요청·응답 DTO다. | 응답에 전체 번호가 포함되지 않도록 유지한다. |
| `server/src/main/resources/db/supabase/V24__payment_methods.sql` | `payment_methods` 테이블 생성 마이그레이션이다. | 이미 Supabase에 적용됨. 기존 파일 수정 대신 다음 번호 마이그레이션을 추가한다. |

## 테스트 파일

| 경로 | 검증 대상 |
| :--- | :--- |
| `server/src/test/java/com/chulsooya/server/domain/order/PaymentControllerTest.java` | 구매자 결제 확인의 주문·멱등 키 규칙 |
| `server/src/test/java/com/chulsooya/server/domain/order/DevelopmentPaymentApprovalServiceTest.java` | 관리자 개발 승인, 비관리자 차단, `PREPARING` 전환 |
| `server/src/test/java/com/chulsooya/server/domain/order/PaymentRefundServiceTest.java` | 관리자 환불·부분 환불·권한 규칙 |
| `server/src/test/java/com/chulsooya/server/domain/paymentmethod/PaymentMethodTest.java` | 결제수단 마지막 네 자리 마스킹 |

## 실제 PG 연동으로 전환할 때 수정 순서

1. PG사를 선정하고 가맹 계약, 테스트 키, 라이브 키를 발급받는다. 기본 일반결제는 카드사·은행 API 직접 연동이 아니라 **PG 결제위젯/결제창**을 사용한다.
2. `CheckoutPage.tsx`에서 PG SDK로 결제창을 연다. PG 호출용 클라이언트 키만 프론트 환경 변수에 둔다.
3. PG 성공 리다이렉트에서 받은 `paymentKey`, `orderId`, `amount`를 철수야 서버의 새 승인 API로 보낸다.
4. `PaymentController.java` 또는 별도 PG 어댑터에서 서버 전용 시크릿 키로 PG 승인 API를 호출한다. 주문 금액과 PG 응답 금액이 같을 때만 `Order.markPaid()`를 호출한다.
5. `Payment.java`에 PG 결제 키·승인 시각·결제수단 마스킹 정보를 저장한다. 시크릿 키·카드 전체 번호·계좌 전체 번호는 DB와 Git에 저장하지 않는다.
6. 새 `PaymentWebhookController.java`를 만들고 PG 웹훅 서명 검증·중복 이벤트 멱등 처리를 구현한다. 웹훅은 최종 상태 대사와 비동기 결제 수단 처리에 사용한다.
7. `DevelopmentPaymentApprovalService.java`와 관리자 **개발 결제 승인** 메뉴는 운영 프로필에서 끄거나 제거한다.
8. PG 승인 성공·실패·취소·부분 취소·웹훅 재전송에 대한 단위·통합 테스트를 추가한다.

## 비밀값 저장 규칙

| 값 | 저장 위치 | Git 포함 여부 |
| :--- | :--- | :--- |
| PG 클라이언트 키 | `client/.env.local` | 금지 |
| PG 시크릿 키 | `server/.env` 또는 배포 환경 변수 | 금지 |
| 웹훅 서명 키 | 서버 환경 변수 | 금지 |
| Supabase 키 | 기존 환경 변수 파일 | 금지 |

## 검증 명령

```powershell
cd client
npm run lint
npx tsc -b
npm run build

cd ..\server
.\gradlew.bat test
.\gradlew.bat bootJar
```

## PG 공식 문서

1. [토스페이먼츠 결제위젯](https://www.tosspayments.com/services/payment-widget)
2. [토스페이먼츠 API 레퍼런스](https://docs.tosspayments.com/reference)
3. [토스페이먼츠 웹훅 연결](https://docs.tosspayments.com/guides/v2/webhook)
4. [토스페이먼츠 웹훅 이벤트](https://docs.tosspayments.com/reference/using-api/webhook-events)
