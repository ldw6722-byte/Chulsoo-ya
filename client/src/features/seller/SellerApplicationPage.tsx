import { useEffect, useRef, useState } from 'react'
import { Link } from 'react-router-dom'
import { ApiError } from '@/api/client'
import { sellerApplicationApi } from '@/api/endpoints'
import { ErrorView, LoadingView } from '@/components/StateViews'
import { useAsync } from '@/hooks/useAsync'
import type { SellerApplication, SubmitSellerApplicationRequest } from '@/types/api'

const INITIAL_FORM: SubmitSellerApplicationRequest = { storeName: '', representativeName: '', businessRegistrationNumber: '', businessOpenedOn: '', cityName: '서울특별시', districtName: '', address: '', phone: '', handledItems: '철물,공구' }
const STATUS: Record<SellerApplication['status'], string> = { PENDING: '심사 대기', MANUAL_REVIEW: '수동 심사', APPROVED: '승인 완료', REJECTED: '반려' }
const MIN_BYTES = 100 * 1024
const MAX_BYTES = 5 * 1024 * 1024
const MIN_EDGE = 800
const MAX_EDGE = 6000

type DocumentKind = 'business' | 'bank'
const LABEL: Record<DocumentKind, string> = { business: '사업자등록증', bank: '통장사본' }

async function validateImage(file: File): Promise<string | null> {
  if (!['image/jpeg', 'image/png'].includes(file.type)) return 'JPG 또는 PNG 이미지만 첨부할 수 있습니다.'
  if (file.size < MIN_BYTES || file.size > MAX_BYTES) return '파일 용량은 100KB 이상 5MB 이하만 가능합니다.'
  const url = URL.createObjectURL(file)
  try {
    const size = await new Promise<{ width: number; height: number }>((resolve, reject) => { const image = new Image(); image.onload = () => resolve({ width: image.naturalWidth, height: image.naturalHeight }); image.onerror = () => reject(new Error('이미지 확인 실패')); image.src = url })
    const shortEdge = Math.min(size.width, size.height); const longEdge = Math.max(size.width, size.height)
    if (shortEdge < MIN_EDGE || longEdge > MAX_EDGE) return '이미지 해상도는 짧은 변 800px 이상, 긴 변 6000px 이하여야 합니다.'
    return null
  } catch { return '이미지 파일을 열 수 없습니다.' } finally { URL.revokeObjectURL(url) }
}

function DocumentDropUpload({ kind, submitted, application, onUploaded }: { kind: DocumentKind; submitted: boolean; application: SellerApplication; onUploaded: (next: SellerApplication) => void }) {
  const [file, setFile] = useState<File | null>(null); const [notice, setNotice] = useState(''); const [saving, setSaving] = useState(false); const [dragging, setDragging] = useState(false); const inputRef = useRef<HTMLInputElement>(null)
  const choose = async (next: File | null) => { if (!next) return; const error = await validateImage(next); if (error) { setFile(null); setNotice(error); return }; setFile(next); setNotice('') }
  const submit = async () => { if (!file) { setNotice(LABEL[kind] + ' 이미지를 선택해 주세요.'); return }; setSaving(true); setNotice(''); try { const next = kind === 'business' ? await sellerApplicationApi.uploadCertificate(application.id, file) : await sellerApplicationApi.uploadBankAccountCopy(application.id, file); onUploaded(next); setFile(null); setNotice(LABEL[kind] + '이 제출되었습니다.'); } catch (error) { setNotice(error instanceof Error ? error.message : LABEL[kind] + ' 제출에 실패했습니다.') } finally { setSaving(false) } }
  return <article className={`rounded-2xl border p-5 ${submitted ? 'border-emerald-200 bg-emerald-50/60' : 'border-amber-200 bg-amber-50/50'}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-black text-slate-900">{LABEL[kind]}</h2><p className="mt-1 text-sm leading-6 text-slate-600">JPG·PNG만 가능하며 100KB 이상 5MB 이하, 짧은 변 800px 이상·긴 변 6000px 이하의 식별 가능한 이미지를 제출해 주세요.</p></div><span className={`rounded-full px-3 py-1 text-xs font-black ${submitted ? 'bg-emerald-100 text-emerald-700' : 'bg-amber-100 text-amber-800'}`}>{submitted ? '제출 완료' : '제출 필요'}</span></div><div onDragEnter={event => { event.preventDefault(); setDragging(true) }} onDragOver={event => event.preventDefault()} onDragLeave={() => setDragging(false)} onDrop={event => { event.preventDefault(); setDragging(false); void choose(event.dataTransfer.files?.[0] ?? null) }} className={`mt-4 rounded-xl border-2 border-dashed px-5 py-6 text-center transition ${dragging ? 'border-brand-500 bg-brand-50' : 'border-slate-300 bg-white'}`}><p className="text-sm font-bold text-slate-700">여기로 이미지를 끌어다 놓으세요</p><p className="mt-1 text-xs text-slate-500">또는 아래에서 파일을 직접 찾을 수 있습니다.</p><input ref={inputRef} type="file" accept="image/jpeg,image/png" onChange={event => void choose(event.target.files?.[0] ?? null)} className="hidden"/><div className="mt-4 flex flex-wrap justify-center gap-2"><button type="button" onClick={() => inputRef.current?.click()} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-bold text-slate-800">파일 찾기</button><button type="button" disabled={!file || saving} onClick={() => void submit()} className="rounded-lg bg-brand-600 px-4 py-2 text-sm font-black text-white disabled:opacity-50">{saving ? '업로드 중' : submitted ? '교체 업로드' : '제출하기'}</button></div>{file ? <p className="mt-3 text-xs font-bold text-slate-600">선택됨: {file.name} · {(file.size / 1024).toFixed(0)}KB</p> : null}</div>{notice ? <p role="status" className="mt-3 text-sm font-medium text-slate-700">{notice}</p> : null}</article>
}
function ApplicationStatus({ application }: { application: SellerApplication }) { return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-black tracking-wider text-brand-600">SELLER APPLICATION</p><h1 className="mt-1 text-2xl font-black text-slate-900">{application.storeName} 판매자 신청</h1><p className="mt-2 text-sm text-slate-500">{application.cityName} {application.districtName} · {application.handledItems.join(', ')}</p></div><span className="rounded-full bg-brand-50 px-3 py-1.5 text-sm font-black text-brand-700">{STATUS[application.status]}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold text-slate-500">사업자등록증</p><p className="mt-1 text-sm font-black text-slate-900">{application.certificateSubmitted ? '제출 완료' : '제출 필요'}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold text-slate-500">통장사본</p><p className="mt-1 text-sm font-black text-slate-900">{application.bankAccountCopySubmitted ? '제출 완료' : '제출 필요'}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold text-slate-500">심사 결과</p><p className="mt-1 text-sm font-black text-slate-900">{application.reviewedAt ? new Date(application.reviewedAt).toLocaleDateString('ko-KR') : '검토 전'}</p></div></div>{application.rejectionReason ? <p className="mt-5 rounded-xl bg-rose-50 p-4 text-sm text-rose-700">반려 사유: {application.rejectionReason}</p> : null}{application.status === 'APPROVED' ? <div className="mt-6 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-800">판매자 계정이 활성화되었습니다. <Link to="/seller" className="font-black underline">판매자 대시보드</Link>에서 운영 설정을 확인해 주세요.</div> : null}</section> }
function DocumentSelectCard({ kind, file, error, onChoose }: { kind: DocumentKind; file: File | null; error: string; onChoose: (file: File | null) => void }) {
  const [dragging, setDragging] = useState(false)
  const inputRef = useRef<HTMLInputElement>(null)
  const choose = (next: File | null) => onChoose(next)
  return <article onDragEnter={event => { event.preventDefault(); setDragging(true) }} onDragOver={event => event.preventDefault()} onDragLeave={() => setDragging(false)} onDrop={event => { event.preventDefault(); setDragging(false); choose(event.dataTransfer.files?.[0] ?? null) }} className={`rounded-2xl border-2 border-dashed p-5 transition ${dragging ? 'border-brand-500 bg-brand-50' : file ? 'border-emerald-300 bg-emerald-50/50' : 'border-slate-300 bg-slate-50'}`}><div className="flex items-start justify-between gap-3"><div><h2 className="font-black text-slate-900">{LABEL[kind]}</h2><p className="mt-1 text-xs leading-5 text-slate-500">JPG·PNG · 100KB~5MB · 짧은 변 800px 이상</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-black ${file ? 'bg-emerald-100 text-emerald-700' : 'bg-rose-100 text-rose-700'}`}>{file ? '선택 완료' : '필수 첨부'}</span></div><input ref={inputRef} type="file" accept="image/jpeg,image/png" onChange={event => choose(event.target.files?.[0] ?? null)} className="hidden"/><div className="mt-5 text-center"><p className="text-sm font-bold text-slate-700">파일을 여기로 끌어다 놓으세요</p><p className="mt-1 text-xs text-slate-500">또는 파일 찾기로 이미지 경로를 선택할 수 있습니다.</p><button type="button" onClick={() => inputRef.current?.click()} className="mt-4 rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-bold text-slate-800">파일 찾기</button></div>{file ? <div className="mt-4 rounded-lg bg-white px-3 py-2 text-xs text-slate-600"><p className="truncate font-bold text-slate-800">{file.name}</p><p className="mt-1">{(file.size / 1024).toFixed(0)}KB</p><button type="button" onClick={() => choose(null)} className="mt-2 text-brand-700 underline">선택 취소</button></div> : null}{error ? <p className="mt-3 text-xs font-bold text-rose-700">{error}</p> : null}</article>
}
function ApplicationForm({ onSubmitted }: { onSubmitted: (application: SellerApplication, notice: string) => void }) {
  const [form, setForm] = useState<SubmitSellerApplicationRequest>(INITIAL_FORM)
  const [files, setFiles] = useState<Record<DocumentKind, File | null>>({ business: null, bank: null })
  const [fileErrors, setFileErrors] = useState<Record<DocumentKind, string>>({ business: '', bank: '' })
  const [notice, setNotice] = useState('')
  const [saving, setSaving] = useState(false)
  const update = <K extends keyof SubmitSellerApplicationRequest>(key: K, value: SubmitSellerApplicationRequest[K]) => setForm(current => ({ ...current, [key]: value }))
  const choose = async (kind: DocumentKind, next: File | null) => { if (!next) { setFiles(current => ({ ...current, [kind]: null })); setFileErrors(current => ({ ...current, [kind]: '' })); return }; const error = await validateImage(next); if (error) { setFiles(current => ({ ...current, [kind]: null })); setFileErrors(current => ({ ...current, [kind]: error })); return }; setFiles(current => ({ ...current, [kind]: next })); setFileErrors(current => ({ ...current, [kind]: '' })) }
  const submit = async () => { const required = ['storeName', 'representativeName', 'businessRegistrationNumber', 'districtName', 'address', 'phone'] as const; if (required.some(key => !form[key].trim())) { setNotice('필수 정보를 모두 입력해 주세요.'); return }; if (!files.business || !files.bank) { setNotice('사업자등록증과 통장사본 이미지를 모두 첨부해 주세요.'); return }; if (fileErrors.business || fileErrors.bank) { setNotice('첨부 이미지 기준을 다시 확인해 주세요.'); return }; setSaving(true); setNotice(''); try { const created = await sellerApplicationApi.submit({ ...form, businessOpenedOn: form.businessOpenedOn || undefined }); try { const afterBusiness = await sellerApplicationApi.uploadCertificate(created.id, files.business); const completed = await sellerApplicationApi.uploadBankAccountCopy(afterBusiness.id, files.bank); onSubmitted(completed, '판매자 신청과 두 증빙 문서 제출이 완료되었습니다. 관리자가 검토합니다.'); } catch (error) { onSubmitted(created, error instanceof Error ? '판매자 신청은 저장됐지만 일부 문서 업로드에 실패했습니다. 아래 제출 영역에서 다시 업로드해 주세요.' : '판매자 신청은 저장됐지만 일부 문서 업로드에 실패했습니다.') } } catch (error) { setNotice(error instanceof Error ? error.message : '판매자 신청에 실패했습니다.') } finally { setSaving(false) } }
  const input = 'h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm outline-none focus:border-brand-500'
  return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-black tracking-wider text-brand-600">SELLER ONBOARDING</p><h1 className="mt-1 text-2xl font-black text-slate-900">판매자 가입 신청</h1><p className="mt-3 text-sm leading-6 text-slate-600">사업자 기본 정보와 필수 증빙 이미지를 한 번에 제출하면 관리자가 검토합니다.</p><div className="mt-6 grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-bold text-slate-700">판매점 이름<input className={input} value={form.storeName} onChange={event => update('storeName', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">대표자명<input className={input} value={form.representativeName} onChange={event => update('representativeName', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">사업자등록번호<input className={input} inputMode="numeric" placeholder="123-45-67890" value={form.businessRegistrationNumber} onChange={event => update('businessRegistrationNumber', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">개업일<input className={input} type="date" value={form.businessOpenedOn ?? ''} onChange={event => update('businessOpenedOn', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">시·도<input className={input} value={form.cityName} onChange={event => update('cityName', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">구·군<input className={input} placeholder="강남구" value={form.districtName} onChange={event => update('districtName', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700 md:col-span-2">사업장 주소<input className={input} value={form.address} onChange={event => update('address', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">연락처<input className={input} inputMode="tel" value={form.phone} onChange={event => update('phone', event.target.value)} /></label><label className="grid gap-2 text-sm font-bold text-slate-700">취급 품목<input className={input} value={form.handledItems ?? ''} onChange={event => update('handledItems', event.target.value)} /></label></div><div className="mt-7 border-t border-slate-100 pt-6"><div><p className="text-xs font-black tracking-wider text-brand-600">REQUIRED DOCUMENTS</p><h2 className="mt-1 text-lg font-black text-slate-900">필수 첨부 서류</h2><p className="mt-2 text-sm text-slate-500">사업자등록증과 통장사본을 모두 선택해야 신청을 접수할 수 있습니다.</p></div><div className="mt-4 grid gap-4 md:grid-cols-2"><DocumentSelectCard kind="business" file={files.business} error={fileErrors.business} onChoose={file => void choose('business', file)} /><DocumentSelectCard kind="bank" file={files.bank} error={fileErrors.bank} onChoose={file => void choose('bank', file)} /></div></div><div className="mt-6 flex flex-wrap items-center justify-between gap-3"><p className="max-w-2xl text-xs leading-5 text-slate-500">증빙 문서는 비공개 저장소에 보관되며, 관리자 심사 목적으로만 조회됩니다.</p><button type="button" disabled={saving} onClick={() => void submit()} className="rounded-xl bg-brand-600 px-6 py-3 text-sm font-black text-white disabled:opacity-50">{saving ? '신청 및 문서 저장 중' : '판매자 신청 접수'}</button></div>{notice ? <p role="status" className="mt-4 rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-700">{notice}</p> : null}</section>
}
export function SellerApplicationPage() { const mine=useAsync<SellerApplication>(()=>sellerApplicationApi.mine(),[]); const [application,setApplication]=useState<SellerApplication|null>(null); const [applicationNotice,setApplicationNotice]=useState(''); useEffect(()=>{if(mine.data)setApplication(mine.data)},[mine.data]);const noApplication=mine.error instanceof ApiError&&mine.error.status===404;if(mine.loading&&!mine.data)return <div className="page"><LoadingView label="판매자 신청 상태를 불러오는 중입니다"/></div>;if(mine.error&&!noApplication&&!application)return <div className="page"><ErrorView error={mine.error} onRetry={mine.reload}/></div>;return <main className="mx-auto max-w-4xl px-4 py-8 md:py-12"><div className="mb-7 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-black tracking-wider text-brand-600">CHULSOO-YA PARTNER</p><h1 className="mt-1 text-3xl font-black tracking-tight text-slate-900">판매자 등록</h1></div><Link to="/my" className="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-bold text-slate-700">마이철수로 돌아가기</Link></div>{application?<><ApplicationStatus application={application}/>{applicationNotice ? <p role="status" className="mt-4 rounded-xl bg-amber-50 px-4 py-3 text-sm text-amber-800">{applicationNotice}</p> : null}<section className="mt-6 grid gap-4 md:grid-cols-2"><DocumentDropUpload kind="business" submitted={application.certificateSubmitted} application={application} onUploaded={setApplication}/><DocumentDropUpload kind="bank" submitted={application.bankAccountCopySubmitted} application={application} onUploaded={setApplication}/></section></>:<ApplicationForm onSubmitted={(next, notice) => { setApplication(next); setApplicationNotice(notice) }} />}</main> }
